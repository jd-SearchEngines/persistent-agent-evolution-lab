[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "persistent-agent-evolution-lab"
version = "0.1.0"
description = "A reproducible lab for testing durable agent memory, skills, and runtime rules."
readme = "README.md"
requires-python = ">=3.11"
license = { file = "LICENSE" }
authors = [{ name = "Persistent Agent Evolution Lab contributors" }]
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0", "ruff>=0.6"]

[tool.hatch.build.targets.wheel]
packages = ["src/persistent_agent"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I"]
